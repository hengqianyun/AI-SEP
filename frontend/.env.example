# 复制为 .env 或 .env.local（勿提交含密钥的文件）

# 浏览器直连后端 API（生产 / 跨域联调必填）
# 例：https://api.example.com/api/v1
VITE_API_BASE_URL=http://127.0.0.1:8080/api/v1

# 静态资源部署子路径（默认 /）
# VITE_BASE=/

# 开发服务器端口
# VITE_DEV_PORT=5173

# 开发态 Vite 代理目标（当页面请求相对路径 /api/* 时）
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8080
