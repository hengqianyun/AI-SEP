# 规划评审

```yaml
reviewId: REV-PLAN-WSC2-R1-uxUiPlanner
planId: PLAN-WSC-2.1
round: 1
role: uxUiPlanner
snapshotIdAtReview: SNAP-WSC-002
decision: REQUEST_CHANGES
summary: |
  相对 PLAN-WSC-2.0，本计划已将导入结果态三入口（重新上传/关闭/失败报告）写入 §3.4 与 TASK-WSC-107 acceptance，
  并继承维护列表 loading/empty/error/success 与 CAT-007 单条/批量验收口径，主路径可落地。
  仍有两处体验验收缺口：① 部分成功与全失败/文件级拒绝未写成可区分的结果态（易照抄原型把「有成功行」标成「导入失败」）；
  ② 2.0 曾要求声明导入为弹窗或独立路由，2.1 §3.1 删除该约束且 102/107 未冻结，与原型「目录页弹窗」可能偏离。
  故 REQUEST_CHANGES；关闭后即可 APPROVE。
```

## 对照结论（本轮焦点）

| 焦点 | 计划覆盖 | 结论 |
|---|---|---|
| UI 状态矩阵 | §3.4「同 2.0 §3.4」+ 导入三结果态可关闭/报告/重传；继承 1.1 主路径矩阵 | 维护列表四态基本够用；**导入四态区分不足**（见 ISSUE-UX-R1-001） |
| 导入交互验收（REQ-CAT-008） | 107：权限、≤10MB、同步 DTO、部分成功、结果态入口；E2E 含失败行+报告 | 入口齐；**部分成功文案/态与文件级拒绝未钉死** |
| 目录维护交互验收（REQ-CAT-007） | 106「同 2.0」：页签/筛选/分页/单条保存取消/批量；E2E 待关联→已维护 | 与原型工具栏、批量栏、单条维护/取消一致，**通过** |
| 与原型一致性 | 侧栏目录维护、三级分类弹窗、导入结果入口方向正确 | **导入载体（弹窗 vs 路由）未声明**（见 ISSUE-UX-R1-002） |

## 异议

| id | severity | evidence | closeWhen | relatedReqs |
|---|---|---|---|---|
| ISSUE-UX-R1-001 | P2 | SNAP OQ-V11-004 / 成功标准要求「部分成功 + 错误报告」；非功能要求关键操作成功/失败可观测。§3.4 虽写「成功/部分失败/全失败」，但继承的 2.0 矩阵仍将「部分失败」挤在 error 列；107 acceptance 写「失败时错误报告入口」，未要求部分成功须同时可观测 successCount+failureCount、且不得仅用「导入失败」标题掩盖成功行。原型 `showImportResult(false)` 在 12 成功/3 失败时标题为「导入失败」，若计划不纠偏，实现易照抄。文件级拒绝（>10MB / 格式）与行级结果态亦未区分。 | 在 §3.4 和/或 TASK-WSC-107 `acceptance`（建议 `testScope` 各抽一条）写明可验收四态：①全成功（可关闭/可重传，无报告亦可）；②部分成功（successCount>0 且 failureCount>0：计数可观测 + 错误报告入口 + 可关闭/重传）；③全失败（failure 计数 + 报告 + 可关闭/重传）；④文件级拒绝（超限/非法格式：停留上传步或等价提示，映射 `ERR_IMPORT_FILE_TOO_LARGE` 等，不伪装成行级结果）。禁止将部分成功仅标为「导入失败」而无成功计数。 | REQ-CAT-008 |
| ISSUE-UX-R1-002 | P2 | 原型 `design/prototypes/wsc-v1.1/index.html`：批量导入为目录页「新增」菜单触发的 `#import-modal` 弹窗（上传步↔结果步、重新上传/关闭）。PLAN-WSC-2.0 §3.1 曾要求「导入为目录页弹窗或独立路由须在清单声明」；PLAN-WSC-2.1 §3.1 删除该行，102/107 亦未冻结载体与入口归属，存在做成独立页或入口漂移、偏离原型与壳层可见性验收的风险。 | 在 §3.1 路由/壳层约定，或 TASK-WSC-102/107 `acceptance` 中显式声明：V1.1 导入采用**目录页弹窗**（对齐原型），或若改为独立路由则写清路径、侧栏/目录入口与关闭后返回点；testScope 含入口可达 + 结果态后关闭回到约定表面。 | REQ-CAT-008, REQ-SHELL-001, REQ-RBAC-001 |

## 非阻塞观察（不升 ISSUE）

- 分类维护「待保存」指示、滚动加载 loading/保位、角色切换后写入口更新：已由 103/104/102「同 2.0」目标与验收覆盖，本轮不重复开单。
- 同步导入（无 job 轮询）简化等待模型；矩阵「上传/解析中」仍适用，107 关闭上述 ISSUE 时可顺带点名提交中禁用/loading。
