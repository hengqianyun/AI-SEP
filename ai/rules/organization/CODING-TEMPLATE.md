# RULE-ORG-CODING 填写模板

> 已填写示例：[CODING-EXAMPLE.md](./CODING-EXAMPLE.md)（仅粒度参考，非默认）
> 架构委员会 / 前后端负责人复制本结构填写 `RULE-ORG-CODING.md`。尖括号内容必须替换；不适用项写 `N/A` 及原因。  
> 本模板不预设 API 风格或框架分层名称；约定须可检查，并尽量指向真实路径或脱敏示例。

## 1. 元数据

```yaml
---
id: RULE-ORG-CODING
version: <语义化版本，如 1.0.0>
status: draft
owner: <架构委员会或 Tech Lead 角色>
override: allowed-with-adr
appliesTo:
  roles: ["developer", "codeReviewer", "apiDataDesigner"]
reviewBy: <YYYY-MM-DD 或 TBD>
---
```

## 2. API / 日志 / 错误码

```markdown
## API

- 风格：`<REST / RPC / GraphQL 等；可说明边界>`
- 契约落盘路径：`<路径或 N/A>`
- 版本策略：`<路径版本 / Header / 其他>`
- 错误响应结构：
  - 字段：`<code / message / correlationId / details 等实际字段名>`
  - 错误码体系：`<前缀、分段规则、稳定标识约定>`
  - 脱敏示例：`<不含真实密钥或 PII 的示例>`
- 鉴权头 / Token 约定：`<头名称与格式；不放真实凭据>`
- 分页约定：`<请求字段、响应字段>`
- 兼容策略：`<兼容变更 / 破坏性变更如何判定>`

## 日志

- 必填字段：`<如 requestId、traceId；是否允许 userId>`
- 禁止字段：`<凭据、完整卡号、明文密码等类别>`
- 级别与用途：`<error/warn/info/debug 使用边界>`
- 关联规则：`<如何把日志与请求/任务关联>`
```

## 3. 前端编码约定

无前端时整节写 `N/A` 及豁免依据。

```markdown
## 前端编码约定

### 文件与目录
- 页面/入口：`<路径与命名规则>`
- 可复用组件：`<路径与准入条件>`
- 业务组件：`<路径与边界>`
- 工具、类型、资源：`<各自路径与放置规则>`

### 组件契约
- 命名：`<文件、组件和公开属性约定>`
- 输入/输出：`<属性、事件或等价接口约定>`
- 复用边界：`<何时提升为共享组件>`
- 样式与设计令牌：`<规则路径或约定；无则 N/A>`

### 状态与数据
- 本地状态边界：`<适用条件>`
- 跨页面/全局状态边界：`<适用条件与统一入口>`
- 服务端数据访问：`<统一客户端/适配层路径>`
- 加载、空、错误、成功状态：`<最低处理要求>`

### 路由与导航
- 路由定义位置：`<路径>`
- 路由命名/元数据：`<约定>`
- 鉴权与重定向：`<统一机制及禁止绕过项>`
```

## 4. 后端与通用代码约定

```markdown
## 代码

- 命名：`<语言侧命名惯例；可分前后端>`
- 分层：`<如入口 → 领域 → 基础设施；按项目实际写>`
- 数据访问：`<允许的入口与禁止直连项>`
- 测试命名与位置：`<约定或引用 QA/Skill>`

## 审查时不可因「个人风格」阻塞的事项

- `<例如已由 formatter/Lint 统一的格式>`
- `<未写入规则且无用户影响的主观写法>`
```

## 5. 例外

```markdown
## 例外

| 规则 | 允许条件 | 批准身份 | 必须留下的证据 |
|---|---|---|---|
| `<规则>` | `<条件>` | `<身份>` | `<ADR/评审记录路径>` |
```

## 6. 完成检查

- [ ] API 错误结构、鉴权头、分页含字段级约定与脱敏示例
- [ ] 日志必填/禁止字段及关联规则可检查
- [ ] 前端编码约定可操作，或已 `N/A`
- [ ] 分层与命名约定与 `RULE-ORG-STACK`、布局规则无冲突
- [ ] 例外需批准与留痕，不能靠局部代码形成第二套规范
- [ ] Owner 将 `status` 改为 `active`
